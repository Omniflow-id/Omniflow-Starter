const { getUserProfilePage } = require("./getUserProfilePage");
const { getChangePasswordPage, changePassword } = require("./changePassword");

module.exports = {
  getUserProfilePage,
  getChangePasswordPage,
  changePassword,
};
