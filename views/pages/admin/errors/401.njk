<!DOCTYPE html>
<html lang="{{ currentLang or 'en' }}">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ t('401.pageTitle') }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="d-flex flex-column min-vh-100">
    <div class="container text-center my-auto">
        <h1 class="display-1 text-info">401</h1>
        <h2 class="mb-4">{{ t('401.title') }}</h2>
        <p class="lead">{{ error.message if error.message else t('401.message') }}</p>
        <div class="mt-4">
            <a href="/admin/login" class="btn btn-primary me-2">{{ t('401.login') }}</a>
            <a href="/" class="btn btn-secondary">{{ t('401.home') }}</a>
        </div>
    </div>
    <footer class="mt-auto text-center py-3">
        <p class="text-muted">&copy; {{ currentYear }} {{ appName }}. {{ t('common.rightsReserved') }}</p>
    </footer>
</body>

</html>