<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rate Limit Exceeded</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="d-flex flex-column min-vh-100">
    <div class="container text-center my-auto">
        <h1 class="display-1 text-danger">429</h1>
        <h2 class="mb-4">Terlalu Banyak Permintaan</h2>
        <p class="lead">{{ errorDescription if errorDescription else "Anda telah mencapai batas permintaan. Silakan tunggu sebentar sebelum mencoba lagi." }}</p>
        
        {% if error_msg and error_msg.length > 0 %}
        <div class="alert alert-warning mt-3">
            {{ error_msg[0] }}
        </div>
        {% endif %}

        <div class="mt-4">
            <a href="javascript:history.back()" class="btn btn-secondary me-2">Kembali</a>
            <a href="/admin" class="btn btn-primary">Dashboard</a>
        </div>
    </div>
    <footer class="mt-auto text-center py-3">
        <p class="text-muted">&copy; {{ currentYear }} Omniflow. All rights reserved.</p>
    </footer>
</body>

</html>