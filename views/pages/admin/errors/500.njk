<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Internal Server Error</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="d-flex flex-column min-vh-100">
    <div class="container text-center my-auto">
        <h1 class="display-1 text-danger">500</h1>
        <h2 class="mb-4">Terjadi Kesalahan Server</h2>
        <p class="lead">{{ error.message if error.message else "Mohon maaf, terjadi kesalahan pada server. Silahkan coba lagi nanti." }}</p>
        <div class="mt-4">
            <a href="javascript:history.back()" class="btn btn-secondary me-2">Kembali</a>
            <a href="/admin" class="btn btn-primary">Dashboard</a>
        </div>
        
        {% if user and user.role == 'Admin' %}
        <div class="mt-4 pt-4 border-top">
            <small class="text-muted">
                <strong>Admin Info:</strong> Error logged dengan timestamp {{ "now" | date("DD/MM/YYYY HH:mm:ss") }}
            </small>
        </div>
        {% endif %}
    </div>
    <footer class="mt-auto text-center py-3">
        <p class="text-muted">&copy; {{ currentYear }} Omniflow. All rights reserved.</p>
    </footer>
</body>

</html>