{% extends "layout/clientMasterLayout.njk" %} {% block content %}
<div class="lang-switcher" style="position: absolute; top: 20px; right: 20px; z-index: 1000;">
  <div class="btn-group shadow-sm" role="group" aria-label="Language switcher">
    {% for lang in languages %}
    <a
      href="{{ lang.href if lang.href else '#' }}"
      class="btn btn-sm btn-light {{ 'active fw-bold text-primary' if lang.isActive else 'text-muted' }}"
      {{ 'aria-current="page"' if lang.isActive else '' }}
      title="{{ lang.name }}"
    >
      <span class="me-1">{{ lang.flag }}</span> {{ lang.shortLabel }}
    </a>
    {% endfor %}
  </div>
</div>

<div class="container-fluid px-4">
  <!-- Hero Section -->
  <div class="row mb-5">
    <div class="col-12">
      <div class="card border-0 shadow-lg bg-primary text-white mt-4">
        <div class="card-body p-5">
          <div class="row align-items-center">
            <div class="col-lg-6 text-center text-lg-start">
              <h1 class="display-4 fw-bold mb-3">{{ appName }}.id</h1>
              <p class="lead mb-4">
                {{ hero.subheading or 'Solusi Digital Terdepan untuk Transformasi Bisnis Anda' }}
              </p>
              <p class="mb-4">
                {{ hero.description or 'Kami menghadirkan inovasi teknologi yang mengoptimalkan alur kerja, meningkatkan efisiensi, dan mendorong pertumbuhan bisnis di era digital.' }}
              </p>
              <div
                class="d-flex flex-column flex-sm-row gap-3 justify-content-center justify-content-lg-start"
              >
                <a href="#services" class="btn btn-light btn-lg px-4"
                  >{{ hero.primaryCta or 'Layanan Kami' }}</a
                >
                <a href="#contact" class="btn btn-outline-light btn-lg px-4"
                  >{{ hero.secondaryCta or 'Hubungi Kami' }}</a
                >
              </div>
            </div>
            <div class="col-lg-6 mt-4 mt-lg-0">
              <div class="text-center">
                <i
                  class="fas fa-rocket opacity-75"
                  style="font-size: clamp(5rem, 15vw, 10rem)"
                ></i>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- About Section -->
  <div class="row mb-5" id="about">
    <div class="col-12">
      <div class="text-center mb-5">
        <h2 class="display-5 fw-bold text-primary">{{ about.title or 'Tentang ' }} {{ appName }}</h2>
        <p class="lead text-muted">
          {{ about.subtitle or 'Membangun masa depan digital yang lebih baik' }}
        </p>
      </div>
    </div>
    <div class="col-lg-4 mb-4">
      <div class="card h-100 border-0 shadow">
        <div class="card-body text-center p-4">
          <div
            class="bg-primary bg-gradient rounded-circle d-inline-flex align-items-center justify-content-center mb-3"
            style="width: 4rem; height: 4rem"
          >
            <i class="fas fa-lightbulb text-white"></i>
          </div>
          <h4 class="fw-bold">{{ about.innovation.title or 'Inovasi' }}</h4>
          <p class="text-muted">
            {{ about.innovation.description or 'Menghadirkan solusi teknologi terdepan yang mengikuti perkembangan zaman dan kebutuhan industri modern.' }}
          </p>
        </div>
      </div>
    </div>
    <div class="col-lg-4 mb-4">
      <div class="card h-100 border-0 shadow">
        <div class="card-body text-center p-4">
          <div
            class="bg-success bg-gradient rounded-circle d-inline-flex align-items-center justify-content-center mb-3"
            style="width: 4rem; height: 4rem"
          >
            <i class="fas fa-users text-white"></i>
          </div>
          <h4 class="fw-bold">{{ about.collaboration.title or 'Kolaborasi' }}</h4>
          <p class="text-muted">
            {{ about.collaboration.description or 'Membangun kemitraan strategis dengan klien untuk mencapai tujuan bisnis bersama melalui pendekatan yang personal.' }}
          </p>
        </div>
      </div>
    </div>
    <div class="col-lg-4 mb-4">
      <div class="card h-100 border-0 shadow">
        <div class="card-body text-center p-4">
          <div
            class="bg-info bg-gradient rounded-circle d-inline-flex align-items-center justify-content-center mb-3"
            style="width: 4rem; height: 4rem"
          >
            <i class="fas fa-chart-line text-white"></i>
          </div>
          <h4 class="fw-bold">{{ about.growth.title or 'Pertumbuhan' }}</h4>
          <p class="text-muted">
            {{ about.growth.description or 'Fokus pada hasil yang terukur dan berkelanjutan untuk mendorong pertumbuhan bisnis jangka panjang.' }}
          </p>
        </div>
      </div>
    </div>
  </div>

  <!-- Services Section -->
  <div class="row mb-5" id="services">
    <div class="col-12">
      <div class="text-center mb-5">
        <h2 class="display-5 fw-bold text-primary">{{ services.title or 'Layanan Kami' }}</h2>
        <p class="lead text-muted">
          {{ services.subtitle or 'Solusi komprehensif untuk kebutuhan digital Anda' }}
        </p>
      </div>
    </div>
    <div class="col-lg-6 mb-4">
      <div class="card border-0 shadow h-100">
        <div class="card-body p-4">
          <div class="d-flex align-items-center mb-3">
            <div
              class="bg-primary bg-gradient rounded-circle d-flex align-items-center justify-content-center me-3"
              style="width: 3rem; height: 3rem"
            >
              <i class="fas fa-code text-white"></i>
            </div>
            <h4 class="fw-bold mb-0">{{ services.appDevelopment.title or 'Pengembangan Aplikasi' }}</h4>
          </div>
          <p class="text-muted mb-3">
            {{ services.appDevelopment.description or 'Membangun aplikasi web dan mobile yang responsif, scalable, dan user-friendly sesuai kebutuhan bisnis Anda.' }}
          </p>
          <ul class="list-unstyled">
            <li>
              <i class="fas fa-check text-success me-2"></i>{{ services.appDevelopment.items.web or 'Web Application Development' }}
            </li>
            <li>
              <i class="fas fa-check text-success me-2"></i>{{ services.appDevelopment.items.mobile or 'Mobile App Development' }}
            </li>
            <li>
              <i class="fas fa-check text-success me-2"></i>{{ services.appDevelopment.items.api or 'API Integration' }}
            </li>
            <li>
              <i class="fas fa-check text-success me-2"></i>{{ services.appDevelopment.items.database or 'Database Design' }}
            </li>
          </ul>
        </div>
      </div>
    </div>
    <div class="col-lg-6 mb-4">
      <div class="card border-0 shadow h-100">
        <div class="card-body p-4">
          <div class="d-flex align-items-center mb-3">
            <div
              class="bg-success bg-gradient rounded-circle d-flex align-items-center justify-content-center me-3"
              style="width: 3rem; height: 3rem"
            >
              <i class="fas fa-cogs text-white"></i>
            </div>
            <h4 class="fw-bold mb-0">{{ services.automation.title or 'Otomasi Proses Bisnis' }}</h4>
          </div>
          <p class="text-muted mb-3">
            {{ services.automation.description or 'Mengoptimalkan alur kerja dengan sistem otomasi yang mengurangi manual work dan meningkatkan efisiensi.' }}
          </p>
          <ul class="list-unstyled">
            <li>
              <i class="fas fa-check text-success me-2"></i>{{ services.automation.items.workflow or 'Workflow Automation' }}
            </li>
            <li>
              <i class="fas fa-check text-success me-2"></i>{{ services.automation.items.document or 'Document Management' }}
            </li>
            <li>
              <i class="fas fa-check text-success me-2"></i>{{ services.automation.items.reporting or 'Reporting System' }}
            </li>
            <li>
              <i class="fas fa-check text-success me-2"></i>{{ services.automation.items.integration or 'Integration Solutions' }}
            </li>
          </ul>
        </div>
      </div>
    </div>
    <div class="col-lg-6 mb-4">
      <div class="card border-0 shadow h-100">
        <div class="card-body p-4">
          <div class="d-flex align-items-center mb-3">
            <div
              class="bg-info bg-gradient rounded-circle d-flex align-items-center justify-content-center me-3"
              style="width: 3rem; height: 3rem"
            >
              <i class="fas fa-chart-bar text-white"></i>
            </div>
            <h4 class="fw-bold mb-0">{{ services.bi.title or 'Business Intelligence' }}</h4>
          </div>
          <p class="text-muted mb-3">
            {{ services.bi.description or 'Mengubah data menjadi insight yang actionable untuk pengambilan keputusan bisnis yang lebih baik.' }}
          </p>
          <ul class="list-unstyled">
            <li>
              <i class="fas fa-check text-success me-2"></i>{{ services.bi.items.analytics or 'Data Analytics' }}
            </li>
            <li>
              <i class="fas fa-check text-success me-2"></i>{{ services.bi.items.dashboard or 'Dashboard Development' }}
            </li>
            <li>
              <i class="fas fa-check text-success me-2"></i>{{ services.bi.items.monitoring or 'Performance Monitoring' }}
            </li>
            <li>
              <i class="fas fa-check text-success me-2"></i>{{ services.bi.items.predictive or 'Predictive Analytics' }}
            </li>
          </ul>
        </div>
      </div>
    </div>
    <div class="col-lg-6 mb-4">
      <div class="card border-0 shadow h-100">
        <div class="card-body p-4">
          <div class="d-flex align-items-center mb-3">
            <div
              class="bg-warning bg-gradient rounded-circle d-flex align-items-center justify-content-center me-3"
              style="width: 3rem; height: 3rem"
            >
              <i class="fas fa-shield-alt text-white"></i>
            </div>
            <h4 class="fw-bold mb-0">{{ services.consulting.title or 'Konsultasi IT' }}</h4>
          </div>
          <p class="text-muted mb-3">
            {{ services.consulting.description or 'Memberikan panduan strategis untuk transformasi digital dan optimasi infrastruktur teknologi.' }}
          </p>
          <ul class="list-unstyled">
            <li>
              <i class="fas fa-check text-success me-2"></i>{{ services.consulting.items.digital or 'Digital Transformation' }}
            </li>
            <li>
              <i class="fas fa-check text-success me-2"></i>{{ services.consulting.items.architecture or 'System Architecture' }}
            </li>
            <li>
              <i class="fas fa-check text-success me-2"></i>{{ services.consulting.items.security or 'Security Assessment' }}
            </li>
            <li>
              <i class="fas fa-check text-success me-2"></i>{{ services.consulting.items.roadmap or 'Technology Roadmap' }}
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>

  <!-- Why Choose Us Section -->
  <div class="row mb-5">
    <div class="col-12">
      <div class="card border-0 shadow-lg bg-light">
        <div class="card-body p-5">
          <div class="text-center mb-5">
            <h2 class="display-5 fw-bold text-primary">
              {{ whyChooseUs.title or 'Mengapa Memilih ' }} {{ appName }} ?
            </h2>
            <p class="lead text-muted">
              {{ whyChooseUs.subtitle or 'Keunggulan yang membedakan kami dari yang lain' }}
            </p>
          </div>
          <div class="row">
            <div class="col-lg-3 col-md-6 mb-4">
              <div class="text-center">
                <div
                  class="bg-primary bg-gradient rounded-circle d-inline-flex align-items-center justify-content-center mb-3"
                  style="width: 4rem; height: 4rem"
                >
                  <i class="fas fa-clock text-white"></i>
                </div>
                <h5 class="fw-bold">{{ whyChooseUs.timing.title or 'Delivery Tepat Waktu' }}</h5>
                <p class="text-muted">
                  {{ whyChooseUs.timing.description or 'Komitmen untuk menyelesaikan proyek sesuai timeline yang disepakati' }}
                </p>
              </div>
            </div>
            <div class="col-lg-3 col-md-6 mb-4">
              <div class="text-center">
                <div
                  class="bg-success bg-gradient rounded-circle d-inline-flex align-items-center justify-content-center mb-3"
                  style="width: 4rem; height: 4rem"
                >
                  <i class="fas fa-medal text-white"></i>
                </div>
                <h5 class="fw-bold">{{ whyChooseUs.quality.title or 'Kualitas Terjamin' }}</h5>
                <p class="text-muted">
                  {{ whyChooseUs.quality.description or 'Standar kualitas tinggi dengan testing dan quality assurance yang ketat' }}
                </p>
              </div>
            </div>
            <div class="col-lg-3 col-md-6 mb-4">
              <div class="text-center">
                <div
                  class="bg-info bg-gradient rounded-circle d-inline-flex align-items-center justify-content-center mb-3"
                  style="width: 4rem; height: 4rem"
                >
                  <i class="fas fa-headset text-white"></i>
                </div>
                <h5 class="fw-bold">{{ whyChooseUs.support.title or 'Support 24/7' }}</h5>
                <p class="text-muted">
                  {{ whyChooseUs.support.description or 'Tim support yang siap membantu kapan saja Anda membutuhkan' }}
                </p>
              </div>
            </div>
            <div class="col-lg-3 col-md-6 mb-4">
              <div class="text-center">
                <div
                  class="bg-warning bg-gradient rounded-circle d-inline-flex align-items-center justify-content-center mb-3"
                  style="width: 4rem; height: 4rem"
                >
                  <i class="fas fa-dollar-sign text-white"></i>
                </div>
                <h5 class="fw-bold">{{ whyChooseUs.pricing.title or 'Harga Kompetitif' }}</h5>
                <p class="text-muted">
                  {{ whyChooseUs.pricing.description or 'Solusi berkualitas dengan harga yang sesuai dengan budget Anda' }}
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Contact Section -->
  <div class="row mb-5" id="contact">
    <div class="col-12">
      <div class="text-center mb-5">
        <h2 class="display-5 fw-bold text-primary">{{ contact.title or 'Hubungi Kami' }}</h2>
        <p class="lead text-muted">
          {{ contact.subtitle or 'Mari diskusikan kebutuhan digital bisnis Anda' }}
        </p>
      </div>
    </div>
    <div class="col-lg-8 mx-auto">
      <div class="card border-0 shadow-lg">
        <div class="card-body p-5">
          <div class="row">
            <div class="col-md-6 mb-4">
              <div class="d-flex align-items-center mb-3">
                <div
                  class="bg-primary bg-gradient rounded-circle d-flex align-items-center justify-content-center me-3"
                  style="width: 3rem; height: 3rem"
                >
                  <i class="fas fa-envelope text-white"></i>
                </div>
                <div>
                  <h5 class="fw-bold mb-1">{{ contact.email or 'Email' }}</h5>
                  <p class="text-muted mb-0">info@omniflow.id</p>
                </div>
              </div>
            </div>
            <div class="col-md-6 mb-4">
              <div class="d-flex align-items-center mb-3">
                <div
                  class="bg-success bg-gradient rounded-circle d-flex align-items-center justify-content-center me-3"
                  style="width: 3rem; height: 3rem"
                >
                  <i class="fas fa-phone text-white"></i>
                </div>
                <div>
                  <h5 class="fw-bold mb-1">{{ contact.phone or 'Telepon' }}</h5>
                  <p class="text-muted mb-0">+62 821 2560 9413</p>
                </div>
              </div>
            </div>
            <div class="col-md-6 mb-4">
              <div class="d-flex align-items-center mb-3">
                <div
                  class="bg-info bg-gradient rounded-circle d-flex align-items-center justify-content-center me-3"
                  style="width: 3rem; height: 3rem"
                >
                  <i class="fas fa-map-marker-alt text-white"></i>
                </div>
                <div>
                  <h5 class="fw-bold mb-1">{{ contact.address or 'Alamat' }}</h5>
                  <p class="text-muted mb-0">Jakarta, Indonesia</p>
                </div>
              </div>
            </div>
            <div class="col-md-6 mb-4">
              <div class="d-flex align-items-center mb-3">
                <div
                  class="bg-warning bg-gradient rounded-circle d-flex align-items-center justify-content-center me-3"
                  style="width: 3rem; height: 3rem"
                >
                  <i class="fas fa-globe text-white"></i>
                </div>
                <div>
                  <h5 class="fw-bold mb-1">{{ contact.website or 'Website' }}</h5>
                  <p class="text-muted mb-0">omniflow.id</p>
                </div>
              </div>
            </div>
          </div>
          <div class="text-center mt-4">
            <a
              href="https://omniflow.id/en/contact"
              target="_blank"
              class="btn btn-primary btn-lg px-5"
            >
              {{ contact.cta or 'Hubungi Kami' }}
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- CTA Section -->
  <div class="row mb-5">
    <div class="col-12">
      <div
        class="card border-0 shadow-lg bg-gradient"
        style="background: linear-gradient(135deg, #0d6efd 0%, #6610f2 100%)"
      >
        <div class="card-body p-5 text-center">
          <h2 class="display-6 fw-bold mb-3">
            {{ cta.title or 'Siap Memulai Transformasi Digital?' }}
          </h2>
          <p class="lead mb-4">
            {{ cta.subtitle or ('Bergabunglah dengan ratusan perusahaan yang telah mempercayai ' + appName + ' untuk mengoptimalkan bisnis mereka.') }}
          </p>
          <a href="#contact" class="btn btn-primary btn-lg px-5"
            >{{ cta.button or 'Mulai Konsultasi Gratis' }}</a
          >
        </div>
      </div>
    </div>
  </div>
</div>
{% endblock %} {% block scripts %}
<script>
  // Smooth scrolling for anchor links
  document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
    anchor.addEventListener("click", function (e) {
      e.preventDefault();
      const target = document.querySelector(this.getAttribute("href"));
      if (target) {
        target.scrollIntoView({
          behavior: "smooth",
          block: "start",
        });
      }
    });
  });
</script>
{% endblock %}
